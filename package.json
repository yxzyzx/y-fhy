{
  "name": "fingertip-daily-booking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "concurrently \"vite\" \"node --env-file=.env --watch server/index.js\"",
    "build": "vite build",
    "start": "node --env-file=.env server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "latest",
    "concurrently": "latest",
    "exceljs": "^4.4.0",
    "express": "latest",
    "vite": "latest",
    "vue": "latest"
  }
}
